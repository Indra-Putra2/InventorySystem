<UserControl x:Class="InventorySystem.View.UserControls.ComboInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventorySystem.View.UserControls"
             mc:Ignorable="d" 
            d:DesignHeight="35" d:DesignWidth="800">
    <Grid Margin="10,10,10,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width=".55*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="0" Text="{Binding Label}" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        <Border Grid.Column="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="BorderBrush" Value="Gray"/>
                    <Setter Property="BorderThickness" Value="1"/>
                    <Setter Property="CornerRadius" Value="2"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsReady}" Value="False">
                            <Setter Property="BorderBrush" Value="Red"/>
                            <Setter Property="BorderThickness" Value="2"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>

            <ComboBox 
                ItemsSource="{Binding Options, UpdateSourceTrigger=PropertyChanged}" 
                SelectedItem="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                ToolTip="{Binding ErrorMessage}"
                ToolTipService.InitialShowDelay="100"
                BorderThickness="0" 
                Background="Transparent"
                FontSize="16"/>
        </Border>
    </Grid>
</UserControl>
