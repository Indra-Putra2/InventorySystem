<UserControl x:Class="InventorySystem.View.UserControls.SpreadSheet"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InventorySystem.View.UserControls"
             xmlns:vm="clr-namespace:InventorySystem.ViewModel.MockData"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:MockInventory, IsDesignTimeCreatable=True}">
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.7*"/>
            <ColumnDefinition Width="0.3*"/>
        </Grid.ColumnDefinitions>

        <DataGrid x:Name="RamData" ItemsSource="{Binding RamDatas}" SelectedItem="{Binding SelectedItem}" Grid.Column="0" IsReadOnly="True" AutoGenerateColumns="False" >
            <DataGrid.Columns>
                <DataGridTextColumn Width="0.35*" Header="Name" Binding="{Binding Name}"></DataGridTextColumn>
                <DataGridTextColumn Width="0.15*" Header="Brand" Binding="{Binding Brand}"></DataGridTextColumn>
                <DataGridTextColumn Width="0.15*" Header="Memory Type" Binding="{Binding MemoryType}"></DataGridTextColumn>
                <DataGridTextColumn Width="0.15*" Header="Module" Binding="{Binding Module}"></DataGridTextColumn>
                <DataGridTextColumn Width="0.2*" Header="Price" Binding="{Binding Price}"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
                
        <Grid Grid.Column="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="0.15*"/>
                <RowDefinition Height="0.75*"/>
                <RowDefinition Height="0.1*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Column="0">
                <TextBlock Grid.Column="0" 
               Text="{Binding ItemName}" 
               FontWeight="Bold"
               FontSize="18"
               TextWrapping="Wrap"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"
               Foreground="Black"/>
            </Grid>
            
            <StackPanel Grid.Row="1" VerticalAlignment="Top">
                <ItemsControl ItemsSource="{Binding ItemDetails}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="3.5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" 
                                   Text="{Binding Label}" 
                                   FontWeight="SemiBold" 
                                   VerticalAlignment="Center" 
                                   Foreground="Gray"/>

                                <TextBlock Grid.Column="1" 
                                   Text="{Binding Value}" 
                                   VerticalAlignment="Center" 
                                   TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            
        </Grid>
    </Grid>
</UserControl>
